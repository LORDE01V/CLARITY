/**
 * Shared brand assets and visual constants used across the UI.
 */

/** Full lockup (mark + wordmark) — headers, auth. */
export const LOGO_URL = "/brand/logo.png";

/** Mark only (overlapping discs) — backgrounds, icons. */
export const MARK_URL = "/brand/mark.svg";

export const BRAND_NAME = "Clarity";
