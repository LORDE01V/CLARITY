# Baked into `vite build` (safe to commit — not secrets).
# Supabase keys stay in Cloudflare Pages build env / local .env only.

VITE_API_BASE_URL=https://clarity-3d3a.onrender.com/api/v1
VITE_AUTH_BYPASS=false
