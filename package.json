{
  "name": "clarity",
  "private": true,
  "scripts": {
    "build": "npm ci --prefix frontend && npm run build --prefix frontend",
    "pages:deploy": "npx wrangler pages deploy frontend/dist --project-name=clarity"
  }
}
